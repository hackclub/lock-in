<div style="display: flex; align-items: center; gap: 1ch;" class="user-info <%= current_user == user ? "current-user" : "" %>">
  <%= image_tag user.avatar_url,
                size: "24x24",
                class: "avatar",
                alt: "#{user.username}'s avatar" if user.avatar_url %>
  <p style="margin: 0;"><%= user.username if user.username %></p>
</div>
